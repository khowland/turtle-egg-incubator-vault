# START

{
  "verdict": "CONFIRM",
  "analysis": "Model 1's diagnosis is correct and well-supported by the evidence. The RPC `vault_finalize_intake` (v9_2_1, lines 133-141) INSERTs an `egg_observation` row with `stage_at_observation='S1'` and the current `session_id` for every egg during intake save. The Observations page (3_Observations.py line 329) builds `observed_ids` by querying `egg_observation` WHERE `session_id`=current AND `is_deleted`=false. Therefore all newly-intaken eggs appear as 'already observed' before the user ever visits the Observations page. The START button (line 522-524) selects only eggs NOT in `observed_ids`, yielding an empty list. The Property Matrix condition at line 579 (`if selected_real_ids:`) evaluates to False, preventing any Property Matrix from rendering. Tests timeout waiting for the stage selectbox that never appears.\n\nThe proposed fix is the root-cause approach: stop auto-creating `egg_observation` rows during intake. The egg table INSERT (RPC line 129) already sets `current_stage='S1'`, which represents the default biological state. The `egg_observation` table should record discrete clinical assessment events, not default states. Removing the auto-S1 creation means eggs arrive on the Observations page truly unobserved, the START button populates `selected_eggs` with all eggs, the Property Matrix renders, and the user's first SAVE action creates the first real `egg_observation` record. My review of the Property Matrix SAVE flow (lines 671-700) confirms it correctly handles first-ever observations. The chalking pre-population (line 629-631) gracefully falls back to `0` when `obs_session` has no prior observations.\n\nI have not found any dependency that requires auto-S1 observations to exist; the bin_observation INSERT (lines 110-121) should remain as it records a real clinical act (weighing the bin during intake).",
  "recommended_fix": {
    "file": "supabase_db/migrations/v9_2_2_REMOVE_AUTO_S1_OBSERVATION.sql",
    "description": "Create a new migration that redefines `vault_finalize_intake` to remove the auto-S1 `egg_observation` INSERT inside the egg creation loop (lines 133-141 of the current RPC). The egg INSERT (lines 126-132) and bin_observation INSERT (lines 110-121) are preserved unchanged.",
    "code_patch": "*** Begin Patch\n*** Create File: supabase_db/migrations/v9_2_2_REMOVE_AUTO_S1_OBSERVATION.sql\n@@\n+BEGIN;\n+\n+-- v9.2.2: Remove auto-S1 egg_observation creation from vault_finalize_intake\n+-- Root Cause: Auto-creating egg_observation rows during intake makes all eggs\n+-- appear 'already observed' on the Observations page, blocking START button\n+-- selection and preventing Property Matrix rendering.\n+-- Fix: Remove the egg_observation INSERT from the egg creation loop.\n+-- The egg's current_stage='S1' already represents the default state.\n+-- egg_observation rows should only be created by user action via Property Matrix.\n+\n+CREATE OR REPLACE FUNCTION public.vault_finalize_intake(p_payload jsonb)\n+RETURNS jsonb\n+LANGUAGE plpgsql\n+SECURITY DEFINER\n+SET search_path = public\n+AS $function$\n+DECLARE\n+  v_species_id text;\n+  v_next_intake int;\n+  v_intake_date date;\n+  v_session_id text;\n+  v_observer_id uuid;\n+  v_observer_name text;\n+  v_intake_id text;\n+  v_bin jsonb;\n+  v_bin_code text;\n+  v_generated_bin_id bigint;\n+  v_notes text;\n+  v_egg_count int;\n+  v_i int;\n+  v_egg_id text;\n+  v_eggs_in_bin int;\n+  v_first_bin bigint;\n+BEGIN\n+  v_species_id := p_payload->>'species_id';\n+  v_intake_date := (p_payload->>'intake_date')::date;\n+  v_session_id := p_payload->>'session_id';\n+  v_observer_id := (p_payload->>'observer_id')::uuid;\n+\n+  SELECT display_name INTO v_observer_name\n+  FROM public.observer\n+  WHERE observer_id = v_observer_id;\n+\n+  IF v_observer_name IS NULL THEN\n+    RAISE EXCEPTION 'vault_finalize_intake: observer_id % not found in observer table', v_observer_id;\n+  END IF;\n+\n+  IF v_species_id IS NULL OR v_session_id IS NULL OR v_observer_id IS NULL THEN\n+    RAISE EXCEPTION 'vault_finalize_intake: missing required payload fields';\n+  END IF;\n+\n+  SELECT intake_count INTO v_next_intake FROM public.species WHERE species_id = v_species_id FOR UPDATE;\n+  IF NOT FOUND THEN\n+    RAISE EXCEPTION 'vault_finalize_intake: species_id % not found', v_species_id;\n+  END IF;\n+\n+  v_next_intake := v_next_intake + 1;\n+  UPDATE public.species SET intake_count = v_next_intake WHERE species_id = v_species_id;\n+\n+  v_intake_id := 'I' || to_char(clock_timestamp(), 'YYYYMMDDHH24MS');\n+\n+  INSERT INTO public.intake (\n+    intake_id, intake_name, finder_turtle_name, species_id, intake_date,\n+    intake_condition, extraction_method, discovery_location,\n+    mother_weight_g, days_in_care, clinical_metadata,\n+    session_id, created_by_id, modified_by_id\n+  ) VALUES (\n+    v_intake_id,\n+    NULLIF(p_payload#>>'{intake,intake_name}', ''),\n+    NULLIF(p_payload#>>'{intake,finder_turtle_name}', ''),\n+    COALESCE(NULLIF(p_payload#>>'{intake,species_id}', '')::text, v_species_id),\n+    COALESCE(NULLIF(p_payload#>>'{intake,intake_date}', '')::date, v_intake_date),\n+    NULLIF(p_payload#>>'{intake,intake_condition}', ''),\n+    NULLIF(p_payload#>>'{intake,extraction_method}', ''),\n+    NULLIF(p_payload#>>'{intake,discovery_location}', ''),\n+    NULLIF(p_payload#>>'{intake,mother_weight_g}', '')::numeric,\n+    (p_payload#>>'{intake,days_in_care}')::int,\n+    COALESCE((p_payload#>>'{intake,clinical_metadata}')::jsonb, '{}'::jsonb),\n+    v_session_id, v_observer_id, v_observer_id\n+  );\n+\n+  v_first_bin := NULL;\n+  FOR v_bin IN SELECT * FROM jsonb_array_elements(COALESCE(p_payload->'bins', '[]'::jsonb))\n+  LOOP\n+    v_bin_code := COALESCE(v_bin->>'bin_code', v_bin->>'bin_id');\n+    v_notes := COALESCE(v_bin->>'bin_notes', 'Clinical Intake Baseline');\n+    v_egg_count := COALESCE((v_bin->>'egg_count')::int, 0);\n+\n+    IF v_bin_code IS NULL OR v_egg_count < 1 THEN\n+      RAISE EXCEPTION 'vault_finalize_intake: invalid bin entry (must have bin_code and 1+ eggs)';\n+    END IF;\n+\n+    INSERT INTO public.bin (\n+      bin_code, intake_id, bin_notes, total_eggs,\n+      target_total_weight_g, substrate, shelf_location,\n+      session_id, created_by_id, modified_by_id\n+    ) VALUES (\n+      v_bin_code, v_intake_id, v_notes, v_egg_count,\n+      (v_bin->>'bin_weight_g')::numeric,\n+      v_bin->>'substrate', v_bin->>'shelf_location',\n+      v_session_id, v_observer_id, v_observer_id\n+    )\n+    RETURNING bin_id INTO v_generated_bin_id;\n+\n+    IF v_first_bin IS NULL THEN\n+      v_first_bin := v_generated_bin_id;\n+    END IF;\n+\n+    -- Create Baseline Bin Observation (records real clinical act: bin weighing at intake)\n+    INSERT INTO public.bin_observation (\n+      bin_observation_id, session_id, bin_id, observer_id, observer_name,\n+      bin_weight_g, incubator_temp_f, env_notes,\n+      created_by_id, modified_by_id\n+    ) VALUES (\n+      'BO-' || v_generated_bin_id || '-' || to_char(clock_timestamp(), 'YYYYMMDDHH24MISS'),\n+      v_session_id, v_generated_bin_id, v_observer_id, v_observer_name,\n+      (v_bin->>'bin_weight_g')::numeric,\n+      (v_bin->>'incubator_temp_f')::numeric,\n+      'Initial Clinical Intake Baseline',\n+      v_observer_id, v_observer_id\n+    );\n+\n+    SELECT count(*)::int INTO v_eggs_in_bin FROM public.egg WHERE bin_id = v_generated_bin_id;\n+    FOR v_i IN 1..v_egg_count LOOP\n+      v_egg_id := v_bin_code || '-E' || (v_eggs_in_bin + v_i);\n+      INSERT INTO public.egg (\n+        egg_id, bin_id, status, current_stage, intake_date,\n+        session_id, created_by_id, modified_by_id\n+      ) VALUES (\n+        v_egg_id, v_generated_bin_id, 'Active', 'S1', v_intake_date,\n+        v_session_id, v_observer_id, v_observer_id\n+      );\n+      -- v9.2.2: REMOVED auto-S1 egg_observation INSERT.\n+      -- Eggs arrive as 'S1' by default (current_stage), but are NOT pre-marked as observed.\n+      -- The first egg_observation will be created by user action via Property Matrix SAVE.\n+    END LOOP;\n+  END LOOP;\n+\n+  RETURN jsonb_build_object(\n+    'intake_id', v_intake_id,\n+    'first_bin_id', v_first_bin\n+  );\n+END;\n+$function$;\n+\n+GRANT EXECUTE ON FUNCTION public.vault_finalize_intake(jsonb) TO service_role;\n+\n+COMMIT;\n*** End Patch"
  },
  "affected_files": [
    "supabase_db/migrations/v9_2_2_REMOVE_AUTO_S1_OBSERVATION.sql (NEW)",
    "vault_views/3_Observations.py (verify SAVE flow for first observation; no code change needed)",
    "vault_views/1_Dashboard.py (verify dashboard stats do not assume egg_observation rows per session)",
    "vault_views/6_Reports.py (verify report filters do not depend on auto-S1 observations)",
    "tests/e2e_playwright/test_observation_workflows.py (retest after applying migration)",
    "tests/e2e_playwright/test_adversarial_observations.py (retest after applying migration)"
  ],
  "risks": "1. Dashboard or Reports views may have queries that count observations per session_id and could undercount if they assume S1 observations always exist. These should be reviewed and fixed if needed — eggs that haven't been clinically observed should not have observation records. 2. The existing egg_observation rows already created by the RPC will remain in the database and continue to block the START button for existing intakes. After deploying the migration, either (a) run a cleanup script to soft-delete auto-S1 observations (`UPDATE egg_observation SET is_deleted=true WHERE observation_notes='Clinical Intake Baseline' AND stage_at_observation='S1'`) or (b) create a new intake to test with. 3. The bin_observation INSERT is preserved so no risk to bin weight recording or environment gate hydration."
}
