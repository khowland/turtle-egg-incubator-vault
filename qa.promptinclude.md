# QA Methodology (auto-injected)
- **KB-First Rule**: Always search 00_CENTRAL_HUB.md and resolved bugs before investigating failures.
- **Mandatory Reporting**: Log every bug found/resolved in tests/resolved_bugs/ using Bug-ID_resolution format. Update 00_CENTRAL_HUB.md.
- **Standard Remediation**:
  - Use `getattr(st.session_state, key, default)` for session state in AppTests.
  - Use `in` for label matching to handle markdown formatting.
  - Use recursive `build_table_aware_mock` from tests/mock_utils.py for deep queries.
- **Testing Standards**:
  - Workflow Mimicry: Test via UI (AppTest) not raw SQL.
  - Full lifecycle coverage: START, SAVE, CANCEL, SOFT DELETE.
  - End-to-End validation: UI feedback + DB verification + UI persistence.
  - Mid-season test data must be generated by executing actual UI workflows.
- **Documentation Alignment**: Always verify changes against Requirements.md and implied_system_objective.md.
