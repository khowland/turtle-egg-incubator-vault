-- WARNING: This schema is for context only and is not meant to be run.
-- Table order and constraints may not be valid for execution.

CREATE TABLE public.bin (
  bin_id text NOT NULL,
  intake_id text,
  bin_date date NOT NULL DEFAULT CURRENT_DATE,
  total_eggs integer CHECK (total_eggs <= 300),
  created_by_session text,
  updated_by_session text,
  is_deleted boolean DEFAULT false,
  deleted_by_session text,
  target_total_weight_g numeric,
  shelf_location text,
  substrate text,
  total_water_added_season_ml numeric DEFAULT 0.0,
  last_moisture_deficit_g numeric,
  created_by_id uuid,
  modified_by_id uuid,
  session_id text,
  bin_notes text,
  created_at timestamp with time zone DEFAULT now(),
  modified_at timestamp with time zone DEFAULT now(),
  CONSTRAINT bin_pkey PRIMARY KEY (bin_id),
  CONSTRAINT bin_mother_id_fkey FOREIGN KEY (intake_id) REFERENCES public.intake(intake_id),
  CONSTRAINT bin_created_by_session_fkey FOREIGN KEY (created_by_session) REFERENCES public.session_log(session_id),
  CONSTRAINT bin_updated_by_session_fkey FOREIGN KEY (updated_by_session) REFERENCES public.session_log(session_id),
  CONSTRAINT bin_deleted_by_session_fkey FOREIGN KEY (deleted_by_session) REFERENCES public.session_log(session_id),
  CONSTRAINT bin_created_by_id_fkey FOREIGN KEY (created_by_id) REFERENCES public.observer(observer_id),
  CONSTRAINT bin_modified_by_id_fkey FOREIGN KEY (modified_by_id) REFERENCES public.observer(observer_id),
  CONSTRAINT bin_session_id_fkey FOREIGN KEY (session_id) REFERENCES public.session_log(session_id)
);
CREATE TABLE public.bin_observation (
  bin_observation_id text NOT NULL,
  session_id text,
  bin_id text,
  timestamp timestamp with time zone DEFAULT now(),
  observer_name text NOT NULL,
  ambient_temp numeric,
  humidity numeric,
  observation_notes text,
  is_deleted boolean DEFAULT false,
  deleted_by_session text,
  bin_weight_g numeric,
  water_added_ml numeric,
  env_notes text,
  created_by_id uuid,
  modified_by_id uuid,
  sub_stage_code text,
  modified_at timestamp with time zone DEFAULT now(),
  CONSTRAINT bin_observation_pkey PRIMARY KEY (bin_observation_id),
  CONSTRAINT incubatorobservation_session_id_fkey FOREIGN KEY (session_id) REFERENCES public.session_log(session_id),
  CONSTRAINT incubatorobservation_bin_id_fkey FOREIGN KEY (bin_id) REFERENCES public.bin(bin_id),
  CONSTRAINT incubatorobservation_deleted_by_session_fkey FOREIGN KEY (deleted_by_session) REFERENCES public.session_log(session_id),
  CONSTRAINT bin_observation_created_by_id_fkey FOREIGN KEY (created_by_id) REFERENCES public.observer(observer_id),
  CONSTRAINT bin_observation_modified_by_id_fkey FOREIGN KEY (modified_by_id) REFERENCES public.observer(observer_id)
);
CREATE TABLE public.biological_property (
  property_id text NOT NULL,
  stage_id text,
  property_label text NOT NULL,
  data_type text DEFAULT 'BOOLEAN'::text,
  is_critical boolean DEFAULT false,
  created_at timestamp with time zone DEFAULT CURRENT_TIMESTAMP,
  modified_at timestamp with time zone DEFAULT CURRENT_TIMESTAMP,
  CONSTRAINT biological_property_pkey PRIMARY KEY (property_id),
  CONSTRAINT biological_property_stage_id_fkey FOREIGN KEY (stage_id) REFERENCES public.development_stage(stage_id)
);
CREATE TABLE public.development_stage (
  stage_id text NOT NULL,
  label text NOT NULL,
  description text,
  created_at timestamp with time zone DEFAULT CURRENT_TIMESTAMP,
  modified_at timestamp with time zone DEFAULT CURRENT_TIMESTAMP,
  milestone text,
  sub_code text,
  ordinal_rank integer,
  CONSTRAINT development_stage_pkey PRIMARY KEY (stage_id)
);
CREATE TABLE public.egg (
  egg_id text NOT NULL,
  bin_id text,
  physical_mark integer,
  current_stage text DEFAULT 'Intake'::text,
  status text DEFAULT 'Active'::text,
  created_by_session text,
  updated_by_session text,
  is_deleted boolean DEFAULT false,
  deleted_by_session text,
  created_at timestamp with time zone DEFAULT CURRENT_TIMESTAMP,
  modified_at timestamp with time zone DEFAULT CURRENT_TIMESTAMP,
  created_by_id uuid,
  modified_by_id uuid,
  session_id text,
  intake_date date,
  egg_notes text,
  last_chalk integer,
  last_vasc boolean,
  last_molding integer DEFAULT 0,
  last_leaking integer DEFAULT 0,
  last_dented integer DEFAULT 0,
  CONSTRAINT egg_pkey PRIMARY KEY (egg_id),
  CONSTRAINT egg_bin_id_fkey FOREIGN KEY (bin_id) REFERENCES public.bin(bin_id),
  CONSTRAINT egg_created_by_session_fkey FOREIGN KEY (created_by_session) REFERENCES public.session_log(session_id),
  CONSTRAINT egg_updated_by_session_fkey FOREIGN KEY (updated_by_session) REFERENCES public.session_log(session_id),
  CONSTRAINT egg_deleted_by_session_fkey FOREIGN KEY (deleted_by_session) REFERENCES public.session_log(session_id),
  CONSTRAINT egg_created_by_id_fkey FOREIGN KEY (created_by_id) REFERENCES public.observer(observer_id),
  CONSTRAINT egg_modified_by_id_fkey FOREIGN KEY (modified_by_id) REFERENCES public.observer(observer_id),
  CONSTRAINT egg_session_id_fkey FOREIGN KEY (session_id) REFERENCES public.session_log(session_id)
);
CREATE TABLE public.egg_observation (
  egg_observation_id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  session_id text,
  egg_id text,
  egg_observation_date timestamp with time zone DEFAULT now(),
  vascularity boolean,
  chalking integer CHECK (chalking >= 0 AND chalking <= 2),
  molding integer DEFAULT 0,
  leaking integer DEFAULT 0,
  observation_notes text,
  is_deleted boolean DEFAULT false,
  deleted_by_session text,
  dented integer DEFAULT 0,
  discolored boolean DEFAULT false,
  moisture_deficit_g numeric,
  water_added_ml numeric,
  stage_at_observation text,
  created_by_id uuid,
  modified_by_id uuid,
  void_reason text,
  sub_stage_code text,
  bin_id text,
  observer_id uuid,
  modified_at timestamp with time zone DEFAULT now(),
  CONSTRAINT egg_observation_pkey PRIMARY KEY (egg_observation_id),
  CONSTRAINT eggobservation_session_id_fkey FOREIGN KEY (session_id) REFERENCES public.session_log(session_id),
  CONSTRAINT eggobservation_egg_id_fkey FOREIGN KEY (egg_id) REFERENCES public.egg(egg_id),
  CONSTRAINT eggobservation_deleted_by_session_fkey FOREIGN KEY (deleted_by_session) REFERENCES public.session_log(session_id),
  CONSTRAINT egg_observation_created_by_id_fkey FOREIGN KEY (created_by_id) REFERENCES public.observer(observer_id),
  CONSTRAINT egg_observation_modified_by_id_fkey FOREIGN KEY (modified_by_id) REFERENCES public.observer(observer_id),
  CONSTRAINT egg_observation_bin_id_fkey FOREIGN KEY (bin_id) REFERENCES public.bin(bin_id),
  CONSTRAINT egg_observation_observer_id_fkey FOREIGN KEY (observer_id) REFERENCES public.observer(observer_id)
);
CREATE TABLE public.hatchling_ledger (
  hatchling_ledger_id uuid NOT NULL DEFAULT gen_random_uuid(),
  egg_id text NOT NULL,
  intake_id text NOT NULL,
  session_id text,
  hatch_date date DEFAULT CURRENT_DATE,
  hatch_weight_g numeric,
  incubation_duration_days integer,
  vitality_score text,
  notes text,
  created_at timestamp with time zone DEFAULT CURRENT_TIMESTAMP,
  modified_at timestamp with time zone DEFAULT CURRENT_TIMESTAMP,
  is_deleted boolean DEFAULT false,
  CONSTRAINT hatchling_ledger_pkey PRIMARY KEY (hatchling_ledger_id)
);
CREATE TABLE public.intake (
  intake_id text NOT NULL,
  intake_name text NOT NULL,
  species_id text,
  intake_date date NOT NULL DEFAULT CURRENT_DATE,
  condition text,
  notes text,
  created_by_session text,
  updated_by_session text,
  is_deleted boolean DEFAULT false,
  deleted_by_session text,
  created_at timestamp with time zone DEFAULT CURRENT_TIMESTAMP,
  modified_at timestamp with time zone DEFAULT CURRENT_TIMESTAMP,
  finder_turtle_name text,
  created_by_id uuid,
  modified_by_id uuid,
  session_id text,
  intake_condition text,
  extraction_method text,
  discovery_location text,
  mother_weight_g numeric,
  clinical_metadata jsonb DEFAULT '{}'::jsonb,
  days_in_care numeric,
  CONSTRAINT intake_pkey PRIMARY KEY (intake_id),
  CONSTRAINT mother_species_id_fkey FOREIGN KEY (species_id) REFERENCES public.species(species_id),
  CONSTRAINT mother_created_by_session_fkey FOREIGN KEY (created_by_session) REFERENCES public.session_log(session_id),
  CONSTRAINT mother_updated_by_session_fkey FOREIGN KEY (updated_by_session) REFERENCES public.session_log(session_id),
  CONSTRAINT mother_deleted_by_session_fkey FOREIGN KEY (deleted_by_session) REFERENCES public.session_log(session_id),
  CONSTRAINT mother_created_by_id_fkey FOREIGN KEY (created_by_id) REFERENCES public.observer(observer_id),
  CONSTRAINT mother_modified_by_id_fkey FOREIGN KEY (modified_by_id) REFERENCES public.observer(observer_id),
  CONSTRAINT mother_session_id_fkey FOREIGN KEY (session_id) REFERENCES public.session_log(session_id)
);
CREATE TABLE public.observer (
  observer_id uuid NOT NULL DEFAULT gen_random_uuid(),
  display_name text NOT NULL UNIQUE,
  is_active boolean DEFAULT true,
  created_at timestamp with time zone DEFAULT now(),
  modified_at timestamp with time zone DEFAULT now(),
  CONSTRAINT observer_pkey PRIMARY KEY (observer_id)
);
CREATE TABLE public.session_log (
  session_id text NOT NULL,
  user_name text NOT NULL,
  login_timestamp timestamp with time zone DEFAULT now(),
  user_agent text,
  CONSTRAINT session_log_pkey PRIMARY KEY (session_id)
);
CREATE TABLE public.species (
  species_id text NOT NULL,
  common_name text NOT NULL UNIQUE,
  scientific_name text NOT NULL UNIQUE,
  incubation_min_days integer,
  incubation_max_days integer,
  optimal_temp_low numeric,
  optimal_temp_high numeric,
  vulnerability_status text,
  species_code character,
  created_at timestamp with time zone DEFAULT CURRENT_TIMESTAMP,
  modified_at timestamp with time zone DEFAULT CURRENT_TIMESTAMP,
  intake_count integer DEFAULT 0,
  CONSTRAINT species_pkey PRIMARY KEY (species_id)
);
CREATE TABLE public.system_config (
  config_key text NOT NULL,
  config_value text,
  description text,
  modified_at timestamp with time zone DEFAULT CURRENT_TIMESTAMP,
  CONSTRAINT system_config_pkey PRIMARY KEY (config_key)
);
CREATE TABLE public.system_log (
  system_log_id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  session_id text,
  event_type text NOT NULL,
  event_message text,
  payload jsonb,
  timestamp timestamp with time zone DEFAULT now(),
  CONSTRAINT system_log_pkey PRIMARY KEY (system_log_id),
  CONSTRAINT systemlog_session_id_fkey FOREIGN KEY (session_id) REFERENCES public.session_log(session_id)
);