# DISCREPANCY_TSK-06

**Task**: TSK-06 — test_adversarial_observations.py
**Status**: HARD_LOCK_DISCREPANCY
**Date**: 2026-05-05

## Discrepancy Description

Per `implied_system_objective.md` §3 (Biological Lifecycle Gaps, S0-S1 Transition), the application **allows non-sequential stage jumps** (e.g., S0 → S5) because the stage selection is user-driven with only a warning, not a backend enforcement. The requirement mandates a biological state machine ensuring eggs pass through S1 (Chalked) before advancing.

This discrepancy makes adversarial testing for Observations invalid or unactionable, because:
- Adversarial tests for stage jump attacks would either succeed (confirming the vulnerability) or be blocked only by the warning (which is not a true guard).
- The task cannot achieve the required DB Pincer assertions (verifying rejection) without the enforcement logic in place.
- Any test written now would document a known deficiency rather than validate correctness.

## Required Resolution

Implement a transition validator in `3_Observations.py` that:
- Prevents stage jumps beyond one step forward/backward (or a defined biological progression).
- Blocks S6 selection unless the previous stage is S5 (or appropriate).
- Logically rejects invalid transitions and provides user feedback.

Once the enforcement is complete, TSK-06 may be reopened.

---
*Generated by Agent Zero per Sprint Directive Discrepancy Stop Rule.*
